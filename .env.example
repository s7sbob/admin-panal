# Copy this file to `.env` and configure the API endpoint for your
# environment. The Vite build system will expose variables prefixed with
# VITE_ to your application via `import.meta.env`.

VITE_API_BASE_URL=https://api.foodifypos.com